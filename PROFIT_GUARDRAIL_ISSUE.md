# گزارش فنی: حل باگ سقف سود و کارهای انجام‌شده (Dynamic Profit Guardrail Fix)

این مستند برای ثبت فنی باگ محاسبه سود کدهای تخفیف/الماس روی محصولات گران‌قیمت (مانند پیش‌خرید GTA VI) و نحوه پیاده‌سازی راه‌حل آن تهیه شده است.

---

## ۱. شرح باگ (The Issue)
پیش از این، وب‌سایت نوبیکس‌شاپ هنگام اعمال کدهای تخفیف بزرگ (مانند کدهای ۲۰٪ برنده‌شده از گردونه) روی محصولات گران‌قیمت با حاشیه سود کم، متضرر می‌شد.

### مثال واقعی: پیش‌خرید GTA VI (ظرفیت هوم ایکس‌باکس یا ظرفیت ۲ پلی‌استیشن):
* **قیمت فروش به مشتری**: ۹,۶۰۰,۰۰۰ تومان
* **قیمت خرید واقعی ما (کاست)**: ۸,۲۰۰,۰۰۰ تومان
* **درصد تخفیف اعمالی**: ۲۰٪ (معادل ۱,۹۲۰,۰۰۰ تومان تخفیف)
* **قیمت پرداختی مشتری بدون محافظت**: ۷,۶۸۰,۰۰۰ تومان
* **میزان ضرر شاپ**: **۵۲۰,۰۰۰- تومان** (فروش زیر قیمت خرید)

### دلایل فنی بروز باگ:
1. **عدم شناسایی قیمت خرید GTA VI**: از آنجا که GTA VI فاقد واریانت‌های عمده‌فروشی در جدول `ResellerPriceTier` بود، سیستم هزینه خرید آن را به طور فال‌بک روی **۸۵٪ قیمت فروش** محاسبه می‌کرد (`۸,۱۶۰,۰۰۰` تومان) و قیمت خرید واقعی دیتابیس (`۸,۲۰۰,۰۰۰` تومان) نادیده گرفته می‌شد.
2. **سقف سود ثابت نامناسب**: سقف سود از پیش تعریف شده به صورت یک عدد ثابت کوچک (`80,000` تومان) بود. این رقم ثابت برای محصولات ارزان مناسب بود ولی روی یک خرید میلیونی عملاً سود ما را به زیر ۱٪ می‌رساند.
3. **عدم پایش الماس‌ها**: الماس‌های وفاداری پایش دقیقی برای حاشیه سود نداشتند و اعمال ترکیبی تخفیف الماس + کد تخفیف منجر به کاهش شدید سود می‌شد.

---

## ۲. راه‌حل و فرمول محاسباتی جدید (The Solution)

تصمیم بر این شد که به جای ریجکت کردن خرید مشتری با خطا (که منجر به رها کردن سبد خرید می‌شد)، تخفیف اعمال‌شده را روی حداکثر مقدار مجاز محدود (Cap) کنیم تا هم خریدار بتواند سفارش خود را نهایی کند و هم حداقل سود مصوب شاپ حفظ شود.

### فرمول پویای کف سود (Profit Floor):
* **سبد خرید زیر ۱ میلیون تومان**: سود خالص باید حداقل **۱۷۰,۰۰۰ تومان** یا **۹٪ قیمت خرید** باشد (هر کدام بزرگتر باشد).
* **سبد خرید بالای ۱ میلیون تومان**: سود خالص باید حداقل **۲۹۰,۰۰۰ تومان** یا **۹٪ قیمت خرید** باشد (هر کدام بزرگتر باشد).

$$\text{Profit Floor} = \begin{cases} 
\max(170,000, 0.09 \times \text{Cost}) & \text{if Price} < 1,000,000 \\
\max(290,000, 0.09 \times \text{Cost}) & \text{if Price} \ge 1,000,000
\end{cases}$$

### سقف تخفیف مجاز (Allowed Discount):
$$\text{Allowed Discount} = \max(0, \text{Gross Amount} - \text{Total Cost} - \text{Profit Floor})$$

---

## ۳. تغییرات در کد (Code Modifications)

تغییرات در دو فایل بک‌بند جنگو پیاده‌سازی شدند:

### ۱) فایل `backend/shop/rewards.py`:
* **شناسایی کاست GTA VI**: در تابع `estimate_item_cost` بررسی می‌شود که اگر محصول `gta6` است، قیمت خرید را مستقیماً از بخش `cost_toman` تنظیمات JSON دیتابیس (`gta6_config`) استخراج کند.
* **محاسبه سود پویا**: در تابع `cap_discount_for_profit` فرمول جدید بر اساس ارزش سفارش پیاده‌سازی شد:
```python
def cap_discount_for_profit(line_items, gross_amount: int, discount_amount: int):
    total_cost = line_items_cost(line_items)
    if int(gross_amount) < 1000000:
        floor = max(170000, int(total_cost * 0.09))
    else:
        floor = max(290000, int(total_cost * 0.09))
    allowed = max(0, int(gross_amount) - total_cost - floor)
    capped = min(int(discount_amount or 0), allowed)
    return capped, total_cost, allowed
```

### ۲) فایل `backend/shop/views.py`:
در فرآیند ثبت نهایی سفارش (`create_order`)، منطق اعمال کدهای تخفیف و الماس‌ها اصلاح شد:
* **محدودسازی تخفیف کد**: تخفیف کد ورودی مشتری با سقف سود بالا مقایسه شده و با استفاده از تابع `min(discount_amount, allowed)` سقف‌گذاری می‌شود.
* **محدودسازی الماس‌ها**: تخفیف الماس‌ها به نحوی محاسبه و سقف‌گذاری می‌شود که مبلغ نهایی پرداختی مشتری از `قیمت خرید + کف سود مصوب` کمتر نشود. همچنین کسر الماس‌ها متناسب با تخفیف نهایی واقعی محاسبه می‌شود تا الماس اضافی کاربر نسوزد.

---

## ۴. اصلاح قیمت‌های خرید دیتابیس (Database Fix)
مقادیر فیلد قیمت خرید (`cost_toman`) برای تمام واریانت‌های GTA VI در تنظیمات دیتابیس اصلاح شد تا سیستم کاست واقعی آن‌ها را بشناسد:
* واریانت‌های Ultimate (PS5 Cap 2 و Xbox Home) روی **۸,۲۰۰,۰۰۰ تومان** ست شدند.
* واریانت‌های فاقد قیمت خرید روی **۸۵٪ قیمت فروش** ست شدند.

---

## ۵. تست و تاییدیه نهایی (Validation)
پس از انجام تغییرات فوق، کلیه تست‌های اتوماتیک با دستور زیر با موفقیت پاس شدند:
```bash
python manage.py test shop
```
سپس فرآیند بک‌سرویس با موفقیت ری‌استارت گردید:
```bash
pm2 restart nubix-backend
```
انجام سفارش‌ها با کدهای تخفیف سنگین و الماس‌ها هم‌اکنون به صورت ۱۰۰٪ ایمن و سودآور کنترل می‌شود.
